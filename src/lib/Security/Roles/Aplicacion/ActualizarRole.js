const NotFoundError=require("../../../../shared/errors/NotFoundError"); class ActualizarRole{constructor(roleRepository){this.roleRepository=roleRepository;} async ejecutar(id,data){const r=await this.roleRepository.findById(id); if(!r) throw new NotFoundError("Rol no encontrado"); return await this.roleRepository.update(id,{code:(data.code??r.code).toUpperCase(), name:data.name??r.name, description:data.description??r.description, isActive:data.isActive??r.isActive});}} module.exports=ActualizarRole;